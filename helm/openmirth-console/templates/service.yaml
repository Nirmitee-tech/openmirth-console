apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmirth-console.fullname" . }}
  labels:
    {{- include "openmirth-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openmirth-console.selectorLabels" . | nindent 4 }}
